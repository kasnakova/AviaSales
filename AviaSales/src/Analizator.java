import java.util.ArrayList;
/**
 * Contains the algorithm for finding suitable routes depending on search parameters
 * @author ildarworld
 *
 */
public class Analizator {
	/**
	 * Searches the available flights and finds the ones that fulfill the search criteria
	 * @param sp the search parameters
	 * @param flights the available flights
	 * @return
	 */
	int i;
	public static ArrayList<Route> searchFlights(SearchParams sp, ArrayList<Flight> flights){
		return null;
	}
	
}
